function doSomething(a,b,c,normalContinuacion, errorContinuacion){
  let r = 0;
  // hacer algun calculo con a b c
  // guardar el resultado en r
  //
  // si ocurrio un error invocar la funcion
  // errorContinuacion("descripcion del error")
  //
  // caso contrario invocar la funcion
  // normalContinuacion(r)
}
